/**
 * PHASE 4B - the financial-input contract version.
 *
 * Separate from contract-runtime.v1 (Phase 4A expression semantics) on purpose:
 * the input contract can evolve without changing how expressions evaluate, and
 * an evaluation result records both identities.
 */
export const FINANCIAL_INPUT_CONTRACT_VERSION = "financial-input-contract.v1";
