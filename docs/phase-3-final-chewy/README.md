# Phase 3 Final — Integrated Chewy Production Validation

**Verdict: `PHASE3_FINAL_COST_BOUND_BEFORE_START`** — zero paid calls, $0.00 spent, Phase 4 not started.

Starting SHA `a474ab3ba42c2518b1dabe552b73dd37712c43e6` (F-7C.1 head). Production files changed: **0**.

## What this mission was

The pre-registered Phase-3 closure gate: compile the whole Chewy 2026 credit agreement through the
production `compileCovenantToIR` entry point (fresh DUAL_PASS_ENSEMBLE Pass A, automatic execution-mode
selection, production sharding, global Pass C, independent verifier), then audit the actual output
against a frozen 46-item human reference set. Hard new-spend cap **$20.00**. Mission §16 requires,
before the first paid call, that the conservative estimate be **≤ $20.00 AND ≤ the current gateway
balance**; if either fails, make zero paid calls and return `PHASE3_FINAL_COST_BOUND_BEFORE_START`
with the exact estimate and call plan. No fix-and-continue; the estimator may not be weakened.

## What was frozen (00)

| Item | Value |
|---|---|
| Starting SHA | `a474ab3ba42c2518b1dabe552b73dd37712c43e6` (verified match) |
| Production compiler tree sha256 | `2c8deb63…8632` |
| Chewy extracted text | `tests/fixtures/unseen-packages/chwy-2026-credit-agreement/extracted-text/doc-a-2026-06-23-credit-agreement.txt` |
| Chewy sha256 | `f63b9dc6560e699cd5158ee0f254dba76da92612ad75fdc27756047ad8f49eeb` (matches Phase 3 validation manifest) |
| Human reference set | `docs/phase-3-validation/04-human-reference-set.json`, sha256 `e7f863e58ee53ca499598f2a8194ff98a0f8f74d2a8ced23b6f12b96c2a64036` |
| Reference items | 46 = 28 CRITICAL + 18 MATERIAL, across 12 semantic units |
| Reference set exposed to compiler/verifier | No (neither ran) |

## Structural / operative preflight (01) — PASS

Deterministic, zero-cost. 1,548 structural nodes, 381 definitions, 0 orphans, 18 roots, single-document
package graph, operative state `null` (no amendment layer, nothing to resolve). All 12 reference units
resolve to unique body nodes. The historical deterministic 3E coverage artifact for this source is
`PACKAGE_SEMANTICALLY_INCOMPLETE`, which is why a fresh whole-agreement run was required in the first place.

## Cost preflight (02) — FAIL against §16

Call plan derived by running every required unit through the certified production shard planner with
an empty inventory (the same planner and budget that produced the certified 36-shard 1.01 plan), then
priced with observed F-5 / F-7B.3E / Phase-3-validation rates.

| Quantity | Value |
|---|---|
| Semantic units required | 12 (1.01, 1.08, 2.09, 2.18, 5.01, 5.18, 6.01, 6.02, 6.03, 6.05, 6.08, 7.01) |
| MONOLITHIC / SHARDED | 1 (6.02) / 11 |
| Projected shards | 88 (1 oversized, in 1.01) |
| Pass A calls (DUAL) | 230 = 2 × (103 batches + 12 gap calls) |
| Pass B conversations | 88 |
| Verifier calls | 12 semantic review + 60 condition-suspicion |
| Mean-rate estimate | **$132.56** (A $97.33 / B $30.60 / verifier $4.64) |
| Worst-observed-rate estimate | $202.26 |
| Conservative (×1.25) | **$252.83** |
| Hard cap | $20.00 |
| Gateway balance | **$12.559204** (read twice, identical; no model call) |
| Cheapest single unit (6.02) | $2.27 mean |
| Largest single unit (1.01) | $65.23 mean |

Conservative ÷ cap = 12.6×. Mean ÷ balance = 10.6×. Section 1.01 alone exceeds both bounds at mean rate.
The bound therefore fails on every reading, not only the conservative one, and no scope that includes
1.01 can fit a $20 cap. Discovery (Phase 2B) output is reused frozen at zero cost; a fresh discovery
would add $4.66 and is disclosed, not hidden.

## Why nothing was run

§16 pre-registers exactly this outcome. Running a partial subset would be a scope change the mission does
not authorise (whole-agreement validation is the deliverable), would be fix-and-continue in spirit, and
could not produce a `PHASE_3_CLOSED` verdict regardless of its result. Ledger (03): 0 calls, balance
12.559204 before and after.

## Artifacts

- `00`–`03`: produced (freeze, structural preflight, cost preflight, ledger). Generated by
  `scripts/phase-3-final-preflight.ts` (zero model calls; one gateway balance read).
- `04`–`12`: **committed as explicit `produced: false` placeholders.** Each requires paid production
  execution, which did not occur. They are present so the §47 list is complete and an absent result cannot
  be mistaken for a lost one.
- `13`: produced. Zero production delta, so the a474ab3 full-suite baseline (107 failing files / 162
  failing tests, identities unchanged) stands. `npm run build` PASS, `npm run lint` clean, `tsc` shows 6
  pre-existing errors in untouched `tests/foundation-audit/*` files and 0 in mission files.
- `14`: closure gate. 31 conditions: 10 PASS, 1 FAIL (condition 6, cost bound), 20 NOT_EVALUATED.

## Recommended next step (not started here)

Either fund the gateway so the balance covers the conservative estimate of the whole-agreement scope, or
open a **new pre-registered mission** with an explicitly narrowed scope and its own cap (per-unit tranches;
everything except 1.01 sums to roughly $67 mean, 1.01 alone ~$65 mean). In both cases re-run the preflight
at the new head so 00–03 are re-frozen before the first paid call. Phase 4 must not begin until a
`PHASE_3_CLOSED` verdict exists.
