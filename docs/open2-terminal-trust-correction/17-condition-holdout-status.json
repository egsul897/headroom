{
  "schemaVersion": "1.0",
  "artifactId": "CONDITION_HOLDOUT_STATUS",
  "disposition": "ENVIRONMENT_BLOCKED",
  "preconditionCheck": {
    "method": "One-shot raw HTTPS probe (POST https://ai-gateway.vercel.sh/v1/messages), credential injected as a process-local environment variable for a single command invocation only, never written to .env or any committed file.",
    "result": "HTTP 402: Team budget exceeded. Current spend: $50.05, limit: $50.00 - identical account state to every prior probe this session.",
    "realCostIncurred": "$0.00"
  },
  "actionTaken": "OPEN-2 came back STILL_OPEN from independent recertification, so per SS24-25 the frozen 22-case holdout was not run (the overall outcome does not hinge on OPEN-4's state when OPEN-2 has not passed). condition-suspicion-classifier.ts and all related production code remain completely untouched this phase, per SS23 (production-frozen pending external Gateway budget restoration).",
  "frozenHoldoutStatus": "Unchanged, untouched, ready to run once budget is restored - unaffected by anything in this phase."
}
