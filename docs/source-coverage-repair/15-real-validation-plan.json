{
  "schemaVersion": 1,
  "artifactId": "15-real-validation-plan",
  "precondition": "BOTH: (a) the \u00a718 fresh independent audit returns no blocker, and (b) the Gateway budget is raised or a fresh credential is supplied.",
  "noNewContent": "Nothing is selected. Every step below runs the already pre-registered manifest from docs/phase3-final-closure.",
  "orderedSteps": [
    {
      "step": 1,
      "what": "targeted Pass-A stability validation",
      "how": "the command in docs/phase3-final-closure/09-targeted-stability-run-1.json, run twice",
      "gate": "semantic inventory stability >= 95% conservative; CRITICAL variable omissions 0",
      "note": "Pass A changed again (v3), so this must be re-run and cannot inherit the v1 numbers"
    },
    {
      "step": 2,
      "what": "whole-agreement run 1, resumed from region 4",
      "how": "scripts/semantic-accountability-validation.ts --resume on the EXACT frozen manifest",
      "note": "regions 1-3 were produced on 976a565 (v1 ids); per-region SHA is disclosed by productionShaByRegion and cross-run comparison is semantic, never by exact id"
    },
    {
      "step": 3,
      "what": "whole-agreement run 2 (all 12 regions)"
    },
    {
      "step": 4,
      "what": "real shared-cap / cross-reference trace",
      "how": "docs/phase3-final-closure/16"
    },
    {
      "step": 5,
      "what": "recompute 11-17 from the run artifacts with the existing scripts"
    }
  ],
  "expectedCostUsd": "~43 at the recorded per-region rates (docs/phase3-final-closure/08)",
  "expectedEffectOfV3": "UNKNOWN and unmeasured on real content. v3 is strictly more likely to report gaps than v2, so the honest prior is that whole-agreement completeness rates will FALL while silent omissions fall to zero. Do not treat a drop in SEMANTICALLY_COMPLETE units as a regression without reading the surfaced spans.",
  "phase3Gates": "unchanged - the existing A-R gate table in docs/phase3-final-closure/23 still governs closure; this mission invents no new gate."
}