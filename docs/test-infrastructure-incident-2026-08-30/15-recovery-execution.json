{
  "schemaVersion": "1.0",
  "artifactId": "RECOVERY_EXECUTION",
  "purpose": "§23: actual execution record, recovery only, no concurrent recertification.",
  "steps": [
    { "step": "Cleaned FWRG's mixed/stale state via exact-companyId-scoped deletes (ClaimReviewItem, ContractRule, DefinedTermDependencyEdge, DefinedTermNode, ContractReferenceEdge, DocumentRelationshipEdge, DocumentNode, ContractCompilerStage/RunDocument/Run) - all scoped to fixture-fwrg-2021-credit-agreement-co only.", "result": "Confirmed via query: docs=1, nodes=0, terms=0, rules=0 before rerun." },
    { "step": "Ran scripts/run-phase-c-compiler.ts fwrg --force in the foreground, uninterrupted, to full completion (this time waited via TaskOutput block:true rather than losing track of a backgrounded process).", "result": "All 11 stages reached a terminal status (8 COMPLETED, 1 REVIEW_REQUIRED, 1 BLOCKED, 1 REVIEW_REQUIRED - all legitimate terminal states, none left RUNNING). Evaluation vs. independent ground truth: 12/18 correct, 5 flagged-incorrect, 0 unflagged-incorrect, 1 missing. DANGEROUS_UNFLAGGED_ERROR_RATE=0.0%. relationshipsPersisted=190, referencesPersisted=24 (both real, non-zero for the first time this incident-response window). Cost: $1.0376, 164324 input / 70897 output tokens." },
    { "step": "Ran scripts/run-phase-c-compiler.ts lsb --force the same way (LSB had no prior state at all - clean slate per 04/14).", "result": "All 11 stages reached a terminal status. Evaluation: 11/14 correct, 2 flagged-incorrect, 0 unflagged-incorrect, 1 missing. DANGEROUS_UNFLAGGED_ERROR_RATE=0.0%. relationshipsPersisted=63, referencesPersisted=9. Cost: $0.4438, 56333 input / 33117 output tokens." },
    { "step": "Combined real paid cost this incident-response window: $1.4814 (FWRG $1.0376 + LSB $0.4438) - within the ~$1.6 estimate given to the user before authorization." },
    { "step": "Ran the full untargeted `npx vitest run` (no path filter).", "result": "240 test files, 2515 tests: ALL PASSING - zero failures anywhere in the repository, including the 2 tests that were failing before this recovery, the new db-isolation-adversarial.test.ts (4 tests), and every known-package/false-credit/tenant-isolation check." },
    { "step": "Repaired the `_prisma_migrations` bookkeeping table gap (present since before this incident-response window began - see 09) via `prisma migrate resolve --applied <name>` for all 30 migrations in chronological order, since the actual schema was already fully up to date (independently confirmed via \\dt and the passing test suite) and only the history-tracking table itself was missing.", "result": "`npx prisma migrate status` now reports 'Database schema is up to date!' - the standing risk of a future real `migrate deploy` failing on already-existing tables is closed." }
  ],
  "noConcurrentRecertification": "No terminal-8-defect recertification work (Part B) ran during this recovery - confirmed by this session's own exclusive, sequential command execution (one foreground/background compiler invocation at a time, gates run only after both compiler runs completed)."
}
