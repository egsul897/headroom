{
  "schemaVersion": "1.0",
  "artifactId": "RECOVERY_TIMELINE",
  "purpose": "§2: ordered reconstruction of everything between DATABASE WIPE DISCOVERED and USER STOPPED EVERYTHING, classified by completion status.",
  "sourcesUsed": ["conversation transcript (this session)", "git log", "Postgres server log /var/log/postgresql/postgresql-16-main.log", "direct DB queries", "vitest output captured in-session"],
  "timeline": [
    { "when": "~18:00-18:20 (session)", "event": "Phase 3F.1.6.RX-FINAL Part A: 7 parallel workstream agents dispatched (A-G), each in its own git worktree, all sharing the ONE physical Postgres instance/database (see 09/10 for why this was the causal defect).", "status": "COMPLETED" },
    { "when": "17:32:56-17:33:16 UTC (Postgres server log)", "event": "4 distinct `prisma_migrate_shadow_db_*` connections force-terminated within 1.5s of each other on the shared instance, immediately followed by a `DROP TABLE _prisma_migrations` that itself failed because the table was ALREADY gone. Two more `DROP TABLE _prisma_migrations` failures recur at 17:44:33 and 17:53:58 - a ~20-minute window of overlapping migrate-tooling resets.", "status": "COMPLETED (this is the wipe event itself)" },
    { "when": "~during Part A integration verification (this session, after the above)", "event": "Full untargeted `npx vitest run` revealed 86 failing tests in tests/solver/coherent-* and related files, root-caused via direct Postgres query to the Company table being at zero rows.", "status": "COMPLETED (discovery)" },
    { "when": "AskUserQuestion #1", "event": "User chose 'Attempt restoration now' over skip/disclose or pause.", "status": "COMPLETED (authorization)" },
    { "when": "re-run with broader exclusion", "event": "`npx vitest run --exclude '**/tests/solver/**'` showed 76 MORE failures - Coherent AND Matthews both wiped, ~15 unrelated historical test files affected.", "status": "COMPLETED (discovery, wider blast radius)" },
    { "when": "AskUserQuestion #2", "event": "User chose 'Attempt full Coherent/Matthews restoration too' over scoping down.", "status": "COMPLETED (authorization)" },
    { "when": "13-script chronological replay", "event": "prisma/seed.ts + 12 populate/backfill scripts for Coherent/Matthews (all confirmed 100% deterministic, zero LLM calls) run in git-introduction order.", "status": "COMPLETED" },
    { "when": "targeted re-verification", "event": "233/236 tests passing across the affected suites; 3 residual failures traced to one missing script.", "status": "COMPLETED" },
    { "when": "14th script", "event": "`scripts/populate-gate0-golden-reconciliation.ts` run - closes 3 of the residual failures.", "status": "COMPLETED" },
    { "when": "immediately after", "event": "Full targeted re-run: 236/236 passing - Coherent+Matthews restoration verified complete.", "status": "COMPLETED" },
    { "when": "FWRG/LSB/CONMED/DSGR §11 investigation", "event": "Confirmed `scripts/phase-3f1-6-known-package-regression.ts` is entirely fixture-file + scratch-company based (creates/tears down its own `phase-3f1-6-known-package-regression-scratch` company every run) and does NOT depend on the persistent fixture-fwrg/lsb companies at all. Ran it: FWRG=0, LSB=0, DSGR=215 (disclosed pre-existing artifact, unchanged), CONMED=N/A - byte-identical to every prior phase. Frozen certification side-effect file reverted via `git checkout --`.", "status": "COMPLETED - this specific §11 regression path required NO restoration" },
    { "when": "full untargeted `npx vitest run` (post Coherent/Matthews fix)", "event": "236 passing except 2 NEW failures in `tests/certification/section11-source-trace-independent.test.ts`, tracing to persistent fixture companies `fixture-fwrg-2021-credit-agreement-co` / `fixture-lsb-2023-abl-credit-agreement-co` (a SEPARATE persistent-DB dependency from the §11 regression script above, holding real ContractRule/DefinedTermNode data from historical Phase C staged-compiler LLM runs).", "status": "COMPLETED (discovery)" },
    { "when": "AskUserQuestion #3", "event": "3 options presented (re-run real compiler ~$1.6, disclose as known gap, leave stray rows+disclose). User chose 're-run real compiler'.", "status": "COMPLETED (authorization)" },
    { "when": "first FWRG compiler attempt", "event": "`npx tsx scripts/run-phase-c-compiler.ts fwrg --force` run with NO credential set - silently used `SyntheticExtractionProvider` fallback (no AI_GATEWAY_API_KEY/ANTHROPIC_API_KEY in this sandbox). Cost $0, 0 real rules extracted, 0/18 matched.", "status": "COMPLETED but INVALID - produced synthetic garbage, not real data" },
    { "when": "cleanup attempt", "event": "Attempted to delete the resulting synthetic-fallback company/document/node rows for fixture-fwrg-2021-credit-agreement-co.", "status": "PARTIALLY_COMPLETED - the sandbox's own auto-mode classifier blocked the bulk Document+Company delete as a destructive action requiring explicit confirmation; the leftover synthetic rows (418 DocumentNode, 41 DefinedTermNode, 0 ContractRule, 1 ContractCompilerRun) remain in the DB, UNCHANGED, at the time of this artifact." },
    { "when": "AskUserQuestion #4", "event": "3 options (clean up + disclose as gap / provide a credential / leave rows + disclose). User chose 'I'll provide a credential'.", "status": "COMPLETED (authorization, contingent on credential)" },
    { "when": "user provides `vck_...` value", "event": "Interpreted as a Vercel AI Gateway key; exported as AI_GATEWAY_API_KEY for a single scoped command invocation (not persisted, not echoed back).", "status": "COMPLETED" },
    { "when": "first retry with credential", "event": "`npx prisma migrate status`-style PrismaClientInitializationError - Postgres itself was DOWN (`pg_isready: no response`; `pg_lsclusters`: down). Root-caused to an unrelated container-idle unclean shutdown (crash-recovery log evidence, see 09) that occurred WHILE waiting for the user's credential, not to any command this session issued.", "status": "COMPLETED (diagnosis)" },
    { "when": "Postgres restart", "event": "`service postgresql start` - WAL crash recovery ran automatically, `pg_isready` confirmed accepting connections again. No data lost by this restart (row counts unchanged before/after).", "status": "COMPLETED, SAFE" },
    { "when": "second retry with credential", "event": "`npx tsx scripts/run-phase-c-compiler.ts fwrg --force` launched, exceeded the 480s foreground timeout, moved to background task b3fytrp2c and left running (real, in-flight, paid LLM calls).", "status": "STARTED, still in flight when the user's next message arrived" },
    { "when": "USER: 'Stop here'", "event": "Explicit interrupt. Session acknowledged and stopped narrating further action; background task b3fytrp2c was left running (not yet stopped) at this exact moment.", "status": "USER STOP BOUNDARY" },
    { "when": "THIS incident-control phase, §0", "event": "task b3fytrp2c (still running, confirmed via ps aux + TaskOutput non-blocking check) stopped via TaskStop before any further inspection.", "status": "COMPLETED" }
  ],
  "summaryClassification": {
    "coherentMatthewsRestoration": "COMPLETED and independently re-verified (236/236 targeted tests) BEFORE the user's STOP - this work is durable and does not need to be redone.",
    "fwrgLsbConmedDsgrSection11KnownPackageRegressionScript": "COMPLETED, NO_ACTION_NEEDED - this script never depended on persistent fixture companies at all.",
    "fwrgLsbPersistentFixtureCompanies": "INTERRUPTED - FWRG's first attempt (synthetic, invalid, not cleaned up) then a second real attempt (in-flight, stopped mid-run by this incident-control phase, not by any error). LSB was never attempted at all this session."
  }
}
