# Phase 3 closure synthesis (final series, after the entity-scope guard)

Regenerated by `scripts/phase-3-closure-synthesis.ts` with `CLOSURE_OUT=docs/phase-3-closure-final` and starting SHA
`995bed37dcc6d1111cec59c966f1d7e1321e71aa`. The earlier series in `docs/phase-3-closure/` (verdict
PHASE3_CLOSURE_EVIDENCE_INSUFFICIENT at `7c18b3e`) is preserved unchanged as history.

## Verdict

**PHASE_3_CLOSED**: 31/31 invariants PASS, 20/20 closure conditions PASS, zero paid calls, Phase 4 not started.

The synthesis derives invariant 31 (entity-scope semantics), closure conditions 13 and 14, the defect-ledger row and
the classification of the blocking verifier finding from `docs/phase-3-entity-scope-final/08-entity-scope-gate.json`
and `05-blocker-replay.json`, after checking that the gate's recorded production file hashes match the current tree.
Nothing is hard-coded to PASS.

## Phase-3 production baseline consumed by Phase 4

Production SHA `5c914558a30e2e1709f16b6d0aaca1bc9bf8e789` (production tree unchanged by the artifact commits that
follow it): `lib/` `806b2e3b6f9c006aae184453c6122a0fbd7d554e`, `lib/contract-model/compiler/`
`b4e6a9da496a23b9f98607355520a456e6c48e1f`, `lib/contract-model/compiler/semantic/`
`f79bc12dd479e9b803bf9e37092d76b6aedb8c12`. Files changed since the previous closure baseline: the entity-scope guard
module, the normalizer wiring and the IR audit type only.

`09-phase4-handoff-contract.json` and `08-product-claim-boundary.json` now carry `inEffect: true` with that SHA.
Phase 4A is not implemented here.

## Regression at this head

Contract-model suites 2499 tests, 0 new failing identities vs base; full suite 3981 tests, 1 new failing identity
which is the previously characterised wall-clock timing flake (isolated reruns pass, direct measurement linear);
tsc 6 pre-existing errors in `tests/foundation-audit/`, 0 new; lint clean; build compiled successfully.
