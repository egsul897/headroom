{
  "schemaVersion": "1.0",
  "artifactId": "CONFIRMED_BUG",
  "exactLocation": "lib/contract-model/compiler/semantic/tools.ts, function resolveNodeWithSupersessionAwareness, line 298 (branch condition) and lines 299-303 (its comment and return)",
  "exactCurrentCode": "if (view.currentText !== null && view.currentText !== undefined) { // A real, resolved amended replacement. Per buildProvisionView's own invariant this only happens when view.status is RESOLVED - ... return { text: view.currentText, textSource: \"AMENDED_CURRENT_TEXT\", provisionOperativeStatus: view.status, nodeSupersessionStatus: nodeSupersession.status, evidenceCurrent: true, unresolvedReasons: [] }; }",
  "confirmedFalseAssumption": "The comment's own claim ('this only happens when view.status is RESOLVED') is not enforced by the code and is not actually guaranteed by buildProvisionView. A real amendment with an unresolved/conditional effective date never enters appliedChain, so the prior/base currentText remains non-null while the aggregate provision status becomes OPERATIVE_STATE_REVIEW_REQUIRED (via AMENDMENT_SEQUENCE_UNRESOLVED). The branch at line 298 checks only currentText presence, unconditionally returns evidenceCurrent: true, and never re-checks view.status - letting a real, independently-reproduced exploit reach persisted VERIFIED trust for getParentClause, getSiblingClauses, and getReferencedProvision (both its absolute and fromNodeId/relative paths).",
  "requiredFix": "Restructure so view.status === 'OPERATIVE_STATE_RESOLVED' is the FIRST, structurally-dominant question whenever a real view exists. currentText presence/absence becomes a secondary, text-selection-only concern nested inside the RESOLVED branch. A non-RESOLVED view must return evidenceCurrent: false regardless of currentText null/non-null, node supersession status, or base text availability.",
  "confirmedByPriorIndependentAuditor": "docs/open2-terminal-trust-correction/13-four-tool-recertification.json and 19-final-verdict.json - a fresh runtime construction (a real, signed amendment with a genuinely unresolved effective-date condition) reproduced this exact gap end-to-end through compile/verify/persist to a real Postgres row."
}
