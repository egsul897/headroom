{
  "schemaVersion": "1.0",
  "artifactId": "DEFINITION_AND_QUALIFIER_GATES_THIRD_RUN",
  "requirement": "A1-A6 definition-exhaustiveness gate (>=5/6) and B1-B4 qualifier gate (4/4 preferred, 3/4 acceptable with one explicitly safe-failed) for this third real holdout execution.",
  "definitionGate": { "seeArtifact": "10-frozen-holdout-rerun.json", "result": "2/6 clean this run (A5, A6); 0/6 silent material omissions (hard gate MET)." },
  "qualifierGateThisRun": {
    "B1_applicableLiquidityThreshold": { "region": "applicable-liquidity-rate", "compiledShape": "MIN(...), sufficiency self-reported COMPLETE", "materialFindingsInOwnTree": "Yes - definitions[0].calculationExpression.operands[0].operands[0]/operands[1] and definitions[0].calculationExpression.operands[1] all flagged UNSUPPORTED_IR_ADDITION/MATERIAL - the SAME class of internal-completeness-overclaim catch as the prior run's B1, now on a differently-shaped (MIN vs IF) but equally real internal gap.", "outcome": "NOT a clean pass, consistent with the prior run's own B1 outcome (not clean either time, for the same underlying reason: the definition's own self-reported COMPLETE sufficiency does not survive independent verification)." },
    "B2_applicableRate": { "region": "applicable-liquidity-rate", "compiledShape": "UNSUPPORTED", "outcome": "NOT a clean pass, consistent with both prior runs." },
    "B3_ebitdaCaps": { "region": "ebitda", "outcome": "NOT a clean pass - same region/reason as A1, consistent with both prior runs; 10%/15% cap language still absent (same source-truncation limitation)." },
    "B4_cashSweepCure": { "region": "cash-sweep-cure", "compiledShape": "This run: Maintenance Liquidity ADD/COMPLETE (consistent), but Deficiency Amount is now UNSUPPORTED/PARTIAL (DIFFERENT from the prior run's own RULE_REFERENCE/COMPLETE) - real model variance in which of the two sibling definitions ends up cleanly linked. 3 real rules still produced (vs 4 in the prior run, vs 2 in the original pre-fix run) - real, if variable, structure captured across all three runs.", "outcome": "NOT a clean pass this run (9 material findings, up from the prior run's own disclosed-as-likely-partly-false-positive 9 findings - similar volume, not further investigated here since 09's own scope-limitation disclosure already covers this class of finding)." }
  },
  "qualifierGateResult": "0/4 clean this run, matching both prior runs numerically. The underlying qualifier-preservation challenge (B1's internal-completeness overclaim, B2's uncaptured pricing grid, B3's source-truncated caps, B4's residual cure-mechanism gap) is REPRODUCIBLE across all three real executions - unlike A1-A6's own claim-level run-to-run flips, B1-B4's own miss pattern is the MOST STABLE finding across this entire multi-session engagement, and should be weighted more heavily than any single A1-A6 numeric snapshot precisely because of that stability.",
  "hardSafetyRequirement": "MET - 0/4 silently trusted material qualifier omissions across all three real executions of this holdout."
}
