# Phase 3 final closure synthesis (zero-cost cumulative evidence gate)

Starting SHA `7c18b3eac834c85539e05b613b0a80b04da02c19`. No model or provider calls, no production changes, no Phase 4 work.
Generated by `scripts/phase-3-closure-synthesis.ts` from committed evidence only.

## Verdict

**PHASE3_CLOSURE_EVIDENCE_INSUFFICIENT** (closure standard 18/20; conditions 13 and 14 fail).

The single blocking item is entity-scope semantics (invariant 31): in the final funded 6.01 run, seven rules carry
`entityScope ["BORROWER"]` while their own provenance excerpts bind or benefit Restricted Subsidiaries, two of those rules
are marked COMPLETE, and no explicit limitation anywhere in the IR prevents a consumer from relying on the scope field.
The deterministic layer (normalize.ts) drops unrecognized entity tags silently, and no check compares an emitted scope
against the binding language of the rule's own excerpt. The raw wire output was not persisted, so whether the model never
emitted the tag or the normalizer dropped it cannot be settled from the frozen evidence.

Single cheapest remediation (named, NOT performed here): a deterministic entity-scope consistency guard
(unrecognized tag becomes a warning plus a PARTIAL sufficiency reason; scope omitting every restricted-subsidiary tag
while the rule's own source binds Restricted Subsidiaries is reset to unspecified with reason
ENTITY_SCOPE_UNDERINCLUSIVE_VS_SOURCE, never widened by guess), replayed at zero cost over the frozen 6.01 IR,
with regression tests, then this synthesis re-run. No paid run is required for the closure decision.

## Artifacts

| # | file | content |
|---|------|---------|
| 01 | `01-evidence-registry.json` | 8 tranches (source accountability, F1-F6, F7, F7C, F7C.1, whole-agreement preflight, 6.01 history, earlier closure attempts) with SHA, artifacts, production version, scope, proves / does-not-prove, status |
| 02 | `02-invariant-matrix.json` | 31 invariants with current evidence, SHA, scope, post-fix flag, limitation, status (30 PASS, 1 FAIL) |
| 03 | `03-defect-closure-ledger.json` | F-1..F-7, HD-1..HD-6, shard lineage loss, certificate semantic gap, entity-scope remediation (15 closed, 1 open) |
| 04 | `04-final-601-critical-missing-audit.json` | the 9 CRITICAL missing items examined one by one (A: 5 bounded true omissions, C: 2, D: 2; 0 unbounded) |
| 05 | `05-final-verifier-findings-audit.json` | 6 verifier findings classified (A, F, C, D, C, D) plus the entity-scope, (b)(14) proviso, 2.50:1.00 and reclassification boundary tests |
| 06 | `06-generalization-and-anti-overfit.json` | production code grep for fixture identifiers: 0 non-comment hits |
| 07 | `07-residual-risk-classification.json` | 15 residual risks (1 BLOCKING_PHASE3, 7 NONBLOCKING_PHASE3, 2 DEFER_TO_PHASE4, 1 DEFER_TO_PHASE5+, 4 TEST_INFRASTRUCTURE) |
| 08 | `08-product-claim-boundary.json` | calibrated product claim (not in effect) |
| 09 | `09-phase4-handoff-contract.json` | draft handoff (not in effect) |
| 10 | `10-regression.json` | targeted 1179/1147/19 (0 new), full 3957/3271/162 (1 new identity = pre-existing timing-measurement flake, isolated 4 pass / 3 fail captured, direct hrtime medians linear), tsc 0 new, lint clean, build clean |
| 11 | `11-phase3-closure-verdict.json` | freeze, 20-condition closure standard, frozen trust and quality facts, verdict, insufficient-evidence block |

## Frozen final-run facts (from artifacts 150-158, not recomputed)

Trust: 7/7 SHARD_COMPLETE, PLANNER_DELIVERY_GAP 0, 14/14 counters MEASURED and 0.
Quality (Pass C): 326 inventoried, 319 material, 294 represented, 6 intentionally non-computational, 8 unsupported,
9 ambiguous, 9 missing from composition (all CRITICAL), 50 quantitative / 0 missing, 0 dangling lineage;
semanticallyComplete=false; status REVIEW_REQUIRED. Reference set: CRITICAL 2 correct / 2 explicit safe limitation /
0 incorrect / 0 silent; MATERIAL 4/0/0/0; B6 REFERENCE_SET_ERROR.
